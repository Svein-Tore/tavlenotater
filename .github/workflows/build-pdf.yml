name: Build LaTeX PDFs with PSTricks (Robust & Fancy HTML)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install LaTeX + PSTricks packages
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            texlive-latex-base \
            texlive-latex-recommended \
            texlive-latex-extra \
            texlive-fonts-recommended \
            texlive-pictures \
            texlive-science \
            texlive-pstricks \
            texlive-publishers \
            dvipng ghostscript

      - name: Find all .tex files
        run: find . -name "*.tex" > texfiles.txt

      - name: Build PDFs (continue on error)
        run: |
          mkdir -p pdfs
          while read file; do
            echo "🧩 Building $file ..."
            base=$(basename "$file" .tex)
            dir=$(dirname "$file")
            if [ "$dir" = "." ]; then
              outdir="pdfs"
            else
              outdir="pdfs/$dir"
            fi
            mkdir -p "$outdir"

            # Build with pdflatex and PSTricks
            if ! pdflatex -shell-escape -interaction=nonstopmode -output-directory="$outdir" "$file"; then
              echo "❌ $file failed"
              touch "$outdir/$base.pdf.failed"
            fi

            # Clean intermediate files
            rm -f "$outdir/$base.aux" "$outdir/$base.log" "$outdir/$base.out" \
                  "$outdir/$base.toc" "$outdir/$base.dvi" "$outdir/$base.ps"
          done < texfiles.txt

      - name: Generate fancy index.html with folder tree and icons
        run: |
          echo '<!DOCTYPE html>
          <html>
          <head>
            <meta charset="UTF-8">
            <title>PDFs</title>
            <style>
              ul { list-style-type: none; margin-left: 1em; }
              li { margin: 0.3em 0; }
              .failed { color: red; font-weight: bold; }
            </style>
          </head>
          <body>
            <h1>PDFs</h1>' > pdfs/index.html

          generate_list() {
            local path="$1"
            echo "<ul>" >> pdfs/index.html
            for f in "$path"/*; do
              if [ -d "$f" ]; then
                name=$(basename "$f")
                echo "<li>📁 <strong>$name</strong></li>" >> pdfs/index.html
                generate_list "$f"
              elif [[ "$f" == *.pdf ]]; then
                rel=${f#pdfs/}
                name=$(basename "$f")
                if [ -f "${f}.failed" ]; then
                  echo "<li>📄 <span class=\"failed\">$name (failed)</span></li>" >> pdfs/index.html
                else
                  echo "<li>📄 <a href='$rel'>$name</a></li>" >> pdfs/index.html
                fi
              fi
            done
            echo "</ul>" >> pdfs/index.html
          }

          generate_list pdfs

          echo '</body></html>' >> pdfs/index.html

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.PAT_TOKEN }}
          publish_dir: ./pdfs
          publish_branch: gh-pages
