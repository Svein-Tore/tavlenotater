name: Test Python Heredoc Workflow

on:
  push:
    branches: [ main ]

jobs:
  test-python:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Minimal Python heredoc test
        run: |
          python3 - <<'PY'
print("Hei fra Python!")
PY
